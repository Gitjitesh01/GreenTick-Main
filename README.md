# AI GreenTick - Main Repository

This is the main repository for AI GreenTick, containing the frontend (Next.js) and the backend (Strapi).

## Directory Structure

- `/frontend`: Next.js application (pages, styling, components, custom dynamic data bindings).
- `/backend`: Strapi v5 Headless CMS (admin panel, SQLite database, dynamic API endpoints).
